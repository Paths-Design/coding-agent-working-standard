id: CAWS-EVIDENCE-REDERIVE-A7-ENV-ORACLE-001
title: 'A7 argv-injection oracle must not depend on an ambient pytest'
risk_tier: 3
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'A7''s shell-metacharacter oracle asserted outcome missing unconditionally, which only holds where the ambient python3 can import pytest. Under the pre-push hook''s leaner PATH the executor correctly returned unavailable and the test failed, blocking the push. Fixed by branching on the HAS_PYTEST guard the file already carried for this exact reason, asserting an exact outcome and detail per environment. Runtime unchanged - unavailable is A13 behaviour working.'
created_at: '2026-09-16T22:53:48.363Z'
updated_at: '2026-09-16T22:56:02.665Z'
created_by_session: '1ee72137-dc4a-41c0-a002-23f2c51c90f9'
blast_radius:
  modules:
    - 'packages/caws-cli/src/store'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/tests/store/evidence-rederive.test.js'
  out: []
invariants:
  - 'The A7 oracle asserts an exact outcome derived from whether the ambient python3 can import pytest; it never accepts a set of outcomes, and it proves in both environments that the hostile nodeid reached the runner as a single literal operand and was never interpreted by a shell.'
acceptance:
  - id: A1
    given: 'the A7 shell-metacharacter test runs on a machine whose ambient python3 cannot import pytest, as under the pre-push hook'
    when: 'the executor reports unavailable rather than missing'
    then: 'the test asserts the exact outcome for the environment it is in and still proves the hostile nodeid was never interpreted, passing both with and without an importable pytest'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Reproduced the pre-push failure and fixed it, proven in both environments on the same commit. ENV A (ambient python3 carries pytest): 25 passed, 0 skipped, asserting outcome missing. ENV B (PATH=nvm-bin:/usr/bin:/bin:/usr/sbin:/sbin so python3 is /usr/bin/python3 and reports No module named pytest): previously 1 failed / 2 skipped, now 23 passed / 2 skipped / 0 failed, asserting outcome unavailable and detail matching No module named pytest. The 2 skips are the pre-existing describeWithPytest block and match the hook run exactly. The oracle asserts one exact outcome per environment rather than a set, so it can still fail for the right reason in either. The runtime was correct and is unchanged."
    recorded_at: "2026-09-16T22:55:53.987Z"
    test_nodeid: packages/caws-cli/tests/store/evidence-rederive.test.js
    commit_sha: bc9e65d2