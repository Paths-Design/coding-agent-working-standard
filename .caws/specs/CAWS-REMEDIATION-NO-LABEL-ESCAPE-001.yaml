id: CAWS-REMEDIATION-NO-LABEL-ESCAPE-001
title: 'Remediation text must not prescribe a label change as the repair'
risk_tier: 2
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge wt-remediation-escape at 38777b050adf2b0c6e5a74f895f469d32260e3c8'
created_at: '2026-09-16T07:47:20.506Z'
updated_at: '2026-09-16T07:58:38.709Z'
created_by_session: '0d83351d-d384-4100-8148-a3f40dbc6b7a'
blast_radius:
  modules:
    - 'TODO: list one or more modules this spec touches.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/kernel/spec/validate-semantics.ts'
    - 'packages/caws-cli/tests/kernel/unit/remediation-no-label-escape.test.ts'
    - packages/caws-cli/src/shell/commands/specs.ts
    - packages/caws-cli/src/store/specs-writer.ts
    - docs/guides/caws-contracts.md
  out: []
invariants:
  - 'TODO: describe one invariant this spec guarantees.'
acceptance:
  - id: A1
    given: 'a tier-gated kernel rule fires'
    when: 'its narrowRepair is read'
    then: 'it names the substantive repair and does not prescribe lowering risk_tier or switching mode as an alternative'
  - id: A2
    given: 'a tier-1 spec with contracts: []'
    when: 'validateSpecSemantics runs'
    then: 'the tier1.contracts_required diagnostic still fires and its narrowRepair names adding a contract without naming risk_tier 3 or mode chore'
  - id: A3
    given: 'an author genuinely chooses risk_tier 3'
    when: 'validateSpecSemantics runs'
    then: 'validation passes, proving the capability was retained and only the prescription removed'
  - id: A4
    given: 'a non-tier-3 spec carrying experimental_mode'
    when: 'the EXPERIMENTAL_MODE_TIER_RESTRICTED diagnostic fires'
    then: 'its narrowRepair names removing experimental_mode and does not prescribe changing risk_tier'
non_functional: {}
contracts:
  - name: 'kernel-spec-diagnostics'
    type: behavior
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Repo-wide scan test 'no narrowRepair in the kernel offers a relabel as the repair' (tests/kernel/unit/remediation-no-label-escape.test.ts). RED FIRST: before the fix it failed listing 4 hits - policy/validate-semantics.ts:76, spec/validate-semantics.ts:36, :47, :103. One (gates.<id>.mode -> 'block') was a FALSE POSITIVE: a gate's mode, prescribing the STRICTER value. Detector narrowed with a (?<![.\\w]) lookbehind and that exact string pinned as a regression test. GREEN AFTER: scan returns []. Non-vacuity guarded by 'the extractor actually finds the kernel remediation corpus' (asserts >50 narrowRepair literals parsed), so a broken regex cannot pass as zero violations."
    recorded_at: "2026-09-16T07:57:42.670Z"
    command: npx jest tests/kernel/unit/remediation-no-label-escape.test.ts
    exit_code: 0
    commit_sha: "3c7c3f02"
  - criterion_id: A2
    status: pass
    evidence_ref: "Live runtime artifact, not just an assertion. Ran the LOCAL dist (node packages/caws-cli/dist/index.js specs create PROBE-1 --title 'Tier two, no contract' --mode feature --risk-tier 2 --scope-in src/x.ts --plan). Output: 'diagnostics: - store.lifecycle.plan_rejected: Tier 2 specs require at least one contract. repair: Declare at least one contract: --contract \"name:api|schema|contract-test|behavior\". The tier is a judgement about blast radius, not a knob for clearing this check.' and 'would_write: no / No files, events, or worktree registry entries were written.' The gate STILL FIRES (rule unchanged) and the repair no longer names a relabel. Also covered by unit tests 'tier 1/2 with contracts: [] still fails with tierN.contracts_required' plus 'the tier-N repair names adding a contract and never a tier or mode change'."
    recorded_at: "2026-09-16T07:57:53.109Z"
    command: node packages/caws-cli/dist/index.js specs create PROBE-1 --mode feature --risk-tier 2 --plan
    exit_code: 0
    commit_sha: "3c7c3f02"
  - criterion_id: A3
    status: pass
    evidence_ref: "Tests 'an author who genuinely chooses tier 3 still validates' and 'an author who genuinely chooses mode: chore still validates at tier 2' (tests/kernel/unit/remediation-no-label-escape.test.ts). Both drive the real validateSpecSemantics over the same fixture with only risk_tier/mode changed, and assert isOk. This is the non-vacuity control for the whole slice: it proves we removed the PRESCRIPTION, not the CAPABILITY. Without it, A1/A2 could be satisfied by simply refusing tier 3 outright, which would be a different and worse change. Full CLI suite 229 suites / 2851 tests pass against a clean tree at 3c7c3f02 (git status --porcelain empty), so dist and committed source agree - this matters because lint-staged reformatted sources AFTER the first build and a stale dist would have tested the wrong bytes."
    recorded_at: "2026-09-16T07:58:03.350Z"
    command: npx jest (full CLI suite)
    exit_code: 0
    commit_sha: "3c7c3f02"
  - criterion_id: A4
    status: pass
    evidence_ref: "Tests 'the rule still fires on a non-tier-3 spec' and 'its repair names removing experimental_mode and never a tier change'. NOTE THIS IS A NEW FINDING, not in Entry 42: the scan surfaced spec/validate-semantics.ts:103 ('Remove experimental_mode or change risk_tier to 3.') as a THIRD instance of the class. Entry 42 measured only the two contract-gate sites (:36 and specs.ts:449). Same shape: a gate that exists to keep experimental_mode off tier 1/2, whose own remediation offers moving the spec to tier 3. Unmeasured - there is no transcript count for how often it was taken, because denials leave no artifact (the unresolved gap this slice does NOT close). Repair now reads: 'Remove experimental_mode from this spec. It is a Tier 3 affordance; a Tier 1/2 slice carries the blast radius that makes it inadmissible.'"
    recorded_at: "2026-09-16T07:58:13.809Z"
    command: npx jest tests/kernel/unit/remediation-no-label-escape.test.ts
    exit_code: 0
    commit_sha: "3c7c3f02"