id: CAWS-HOOKPACK-BASH32-EMPTY-HANDLER-CI-REGRESSION-001
title: 'Blocking macOS bats regression guard for the bash 3.2 empty-handler-array crash'
risk_tier: 3
mode: chore
lifecycle_state: active
worktree: hooks-bash32-ci-regression
created_at: '2026-09-08T01:26:16.818Z'
updated_at: '2026-09-08T01:26:32.469Z'
created_by_session: '96a9b85d-e209-45ad-9efb-d8522a340f79'
blast_radius:
  modules:
    - 'caws-cli/tests/hooks/bats'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/tests/hooks/bats/empty-handler-array-bash32.bats'
    - '.github/workflows/pr-checks.yml'
  out: []
invariants:
  - 'A PR-blocking CI job runs on a real macOS runner''s /bin/bash and fails loudly (not skips) if that bash is not the historically vulnerable 3.2.x, so the empty-handler-array regression test can never silently become a dead instrument.'
acceptance:
  - id: A1
    given: 'the shared run_handlers library and post_tool_use.sh dispatch script'
    when: 'a PR-blocking bats job runs on macos-latest against /bin/bash'
    then: 'a positive-control test proves /bin/bash is 3.2.x and that an empty array under set -u crashes on it, and candidate tests prove run_handlers and post_tool_use.sh with every current handler disabled both exit 0'
  - id: A2
    given: 'a future edit reintroduces the unguarded  expansion'
    when: 'the macOS bats job runs on a PR'
    then: 'the candidate test fails (proving the regression guard actually detects the reintroduced defect, not just a vacuous pass)'
non_functional: {}
contracts: []
