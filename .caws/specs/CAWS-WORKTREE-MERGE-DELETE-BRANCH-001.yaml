id: CAWS-WORKTREE-MERGE-DELETE-BRANCH-001
title: 'worktree merge leaves the merged branch behind: delete it with git branch -d inside the merge sequence'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-07-25T03:54:18.942Z'
updated_at: '2026-07-25T03:54:18.942Z'
blast_radius:
  modules:
    - 'TODO: list one or more modules this spec touches.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/store/worktrees-writer.ts'
    - 'packages/caws-cli/src/shell/commands/worktree.ts'
    - 'packages/caws-cli/tests'
  out: []
invariants:
  - 'TODO: describe one invariant this spec guarantees.'
acceptance:
  - id: A1
    given: 'a bound worktree whose branch merges cleanly'
    when: 'caws worktree merge runs'
    then: 'the branch is deleted with git branch -d and the deletion is reported in the existing success line'
non_functional: {}
contracts:
  - name: 'merge-deletes-merged-branch'
    type: behavior
