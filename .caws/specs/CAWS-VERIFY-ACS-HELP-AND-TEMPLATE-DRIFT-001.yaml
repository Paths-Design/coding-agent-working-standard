id: CAWS-VERIFY-ACS-HELP-AND-TEMPLATE-DRIFT-001
title: 'Derive the executable-runner split into --help, correct the pytest resolution claim, and unstale the shipped removed-command templates'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-09-16T23:33:40.123Z'
updated_at: '2026-09-16T23:42:51.387Z'
created_by_session: '1ee72137-dc4a-41c0-a002-23f2c51c90f9'
blast_radius:
  modules:
    - 'packages/caws-cli/src/shell'
    - 'packages/caws-cli/templates'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/shell/command-metadata.ts'
    - 'packages/caws-cli/src/store/evidence-rederive.ts'
    - 'packages/caws-cli/templates/CLAUDE.md'
    - 'packages/caws-cli/templates/agents.md'
    - 'packages/caws-cli/tests/shell/verify-acs-help-surface.test.js'
    - 'packages/caws-cli/CHANGELOG.md'
    - 'AGENTS.md'
    - packages/caws-cli/tests/shell/specs-verify-acs.test.js
    - packages/caws-cli/tests/store/evidence-rederive.test.js
  out: []
invariants:
  - 'The set of runners re-derivation can execute is exported once; the dispatch switch and the --help description both derive from it, so implementing a new runner cannot leave the help stale.'
  - 'No command named in a shipped template''s removed-commands list may be registered on the live command surface; a test enforces this against COMMAND_SURFACE_METADATA.'
acceptance:
  - id: A1
    given: 'the shipped templates list removed v10 commands'
    when: 'a command in that list is registered on the live surface'
    then: 'a test fails naming it, and verify-acs is absent from both removed-lists'
  - id: A2
    given: '--runner accepts pytest|jest|vitest|cargo|go but only pytest and jest can execute'
    when: 'a reader consults --help or the generated command reference'
    then: 'the executable and detected-only sets are named separately and both the help text and the dispatch switch derive from one exported constant'
  - id: A3
    given: 'jest is resolved from the repo node_modules/.bin but pytest is invoked as ambient python3 -m pytest'
    when: 'a reader consults the --run description'
    then: 'it states the pytest mechanism accurately instead of claiming both are repo-resolved'
  - id: A4
    given: 'global agent doctrine instructs recording evidence with --verify'
    when: 'an agent reads the repo agent docs'
    then: 'AGENTS.md documents caws specs evidence --verify'
non_functional: {}
contracts:
  - name: 'command-surface-help'
    type: behavior
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "templates/CLAUDE.md and templates/agents.md no longer list verify-acs among v10 commands 'removed in v11.0 and are not coming back'; both now state the 12.2 restoration as caws specs verify-acs, and annotate validate with caws specs validate <file>. The whole new suite is the citation because the two template pins are parameterised per file (template-literal titles); the file-level nodeid fails if either template regresses. The pin is scoped to caws specs leaves deliberately - a whole-surface collision scan failed on 'plan' because caws scope plan shares a word with v10's unrelated caws plan, and asserting on that would only teach the next author to silence the test."
    recorded_at: "2026-09-16T23:50:31.706Z"
    test_nodeid: packages/caws-cli/tests/shell/verify-acs-help-surface.test.js
    commit_sha: "671b70f322dcb03c3f24612ca0c2841e8fe2929b"
  - criterion_id: A2
    status: pass
    evidence_ref: "EXECUTABLE_TEST_RUNNERS (src/store/evidence-rederive.ts) is the single source: testOutcome's dispatch branches on isExecutableRunner() instead of hand-listed fall-through cases, and the --runner description is computed from the same constant plus its complement. Rendered help on the rebuilt dist: 'Override test-runner detection. Only pytest, jest execute under --run; naming vitest, cargo, go reports unavailable and verifies nothing: pytest | jest | vitest | cargo | go'. The cited test parses the two name lists back OUT of the rendered prose and compares them to the constants, so replacing the computed description with a literal fails. Two sibling tests in the same file complete the proof and are NOT part of this mechanical citation: 'a detected-only runner reports unavailable and spawns nothing' drives all five selectable runners through the real compiled executor and asserts the detected-only ones spawn no subprocess at all, and 'the built CLI prints both halves of the split' asserts against node dist/index.js output because register.ts opt-forwarding lets metadata be right while the help a user sees is not."
    recorded_at: "2026-09-16T23:50:47.239Z"
    test_nodeid: "packages/caws-cli/tests/shell/verify-acs-help-surface.test.js::the description names each executable runner and each detected-only one"
    commit_sha: "671b70f322dcb03c3f24612ca0c2841e8fe2929b"