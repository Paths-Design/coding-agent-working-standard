id: CAWS-HOOKPACK-AGENT-SURFACE-MACHINE-RUNTIME-HOME-CONTROL-001
title: 'Behavioral control for caws_source_lib''s machine-runtime home tier'
risk_tier: 3
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge machine-runtime-home-control at 73815a45e6c288639b4a61932d6e44f215874516'
created_at: '2026-09-08T04:30:58.615Z'
updated_at: '2026-09-08T04:46:43.879Z'
created_by_session: '96a9b85d-e209-45ad-9efb-d8522a340f79'
blast_radius:
  modules:
    - 'packages/caws-cli/tests/hooks/bats'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/tests/hooks/bats/home-unset-guard-robustness.bats'
  out: []
invariants:
  - 'The machine-runtime home-tier override in caws_source_lib is exercised by an executed behavioral test, not only a source-pattern grep'
acceptance:
  - id: A1
    given: 'CAWS_MACHINE_RUNTIME=1, CAWS_MACHINE_LIBRARIES=''{}'', and HOME/CAWS_HOME both absent'
    when: 'caws_source_lib sources a basename'
    then: 'it falls through to CAWS_MACHINE_ADAPTER_LIB_DIR without error -- proving the user-home tier is genuinely skipped, not silently matched against a root path'
  - id: A2
    given: 'a real HOME containing .caws/surfaces/<surface>/lib/<basename> AND a CAWS_MACHINE_ADAPTER_LIB_DIR containing a same-named file'
    when: 'caws_source_lib sources that basename'
    then: 'the user-home override wins over the adapter fallback -- proving the legitimate machine-runtime user tier still functions after the authority-alias fix'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "bats: 'HOSTILE: caws_source_lib's machine-runtime user tier is skipped (not root-probed) when HOME and CAWS_HOME are absent' -- outcome alone is vacuous (both buggy/fixed fall through to adapter since a real root path can't be created without root), so the test asserts on the bash -x TRACE; reverting agent-surface.sh to 3f41fbed reproduced the trace line 'local _machine_user=/.caws/surfaces/claude-code/lib/probe.sh' and 'not ok'; fixed code never constructs it."
    recorded_at: "2026-09-08T04:46:02.881Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "bats: 'POSITIVE CONTROL: caws_source_lib's machine-runtime user tier still wins over the adapter fallback under a real HOME' passes against both the reverted and fixed agent-surface.sh (expected -- the bug was specific to the unset-HOME path, so the real-HOME legitimate case was never broken). Full suites clean: npx jest -> 2531/2531 (/tmp/jest-machine-runtime.log); npx bats tests/hooks/bats -> 207/207 (/tmp/bats-machine-runtime.log)."
    recorded_at: "2026-09-08T04:46:05.676Z"