id: CAWS-HELP-CLAIM-DECLARED-NOT-DESCRIBED-001
title: 'Option value sets and version claims are declared in code, not described in help prose'
risk_tier: 3
mode: fix
lifecycle_state: active
created_at: '2026-09-17T01:28:02.638Z'
updated_at: '2026-09-17T01:37:35.077Z'
created_by_session: '1ee72137-dc4a-41c0-a002-23f2c51c90f9'
blast_radius:
  modules:
    - 'caws-cli shell command surface'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/shell/command-metadata.ts'
    - 'packages/caws-cli/src/shell/commands/specs.ts'
    - 'packages/caws-cli/src/shell/commands/events.ts'
    - 'packages/caws-cli/src/shell/commands/worktree.ts'
    - 'packages/caws-cli/src/store/migration-versions.ts'
    - 'packages/caws-cli/tests/shell/help-claim-integrity.test.js'
    - 'packages/caws-cli/CHANGELOG.md'
    - packages/caws-cli/src/shell/register.ts
    - packages/caws-cli/mutation-policy.json
    - packages/caws-cli/tests/ci/mutation-contract.test.js
  out: []
invariants:
  - 'A closed accepted-value set is declared once as an exported constant; help text and runtime validation both read it, so neither can name a set the other refuses'
  - 'No user-facing string (help description or error message) names a CAWS version number; version references go stale silently and nothing checks them'
acceptance:
  - id: A1
    given: '--from <version> on specs migrate and events migrate accepts exactly v10'
    when: 'a reader consults --help or triggers the rejection error'
    then: 'the accepted set and both error messages derive from one exported constant, and no user-facing string names a CAWS version'
  - id: A2
    given: 'worktree prune --state accepts a closed set already held in WORKTREE_PRUNE_STATES'
    when: 'a reader consults --help'
    then: 'the description''s value list derives from that exported constant rather than a hand-written example'
  - id: A3
    given: 'four help descriptions claim specific exit codes'
    when: 'the built CLI is invoked for each claimed condition'
    then: 'the observed exit code matches the claim'
  - id: A4
    given: 'help prose that names a CAWS version goes stale silently'
    when: 'the option surface is linted'
    then: 'no option description references a CAWS version number, and any option naming a closed accepted set carries allowedValues'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "MIGRATABLE_SOURCE_VERSIONS (src/store/migration-versions.ts) now holds the accepted set. Both --from options carry it as allowedValues, and both rejection messages read describeMigratableSourceVersions(), so help and guard cannot disagree. No user-facing string names a release number: 'only v10 is supported in v11.2' shipped in four of them while the package was at 12.2.0-rc.2. THIRD COPY FOUND ONLY BY INVOKING DIST: register.ts:868 carried a duplicate --from guard that shadowed the handler's check AND forwarded a hardcoded 'v10', so the handler's validation was unreachable and its message never reached a caller - fixing the handler alone changed nothing observable. That is the register.ts opt-forward class; a handler unit test goes green over it. The parse-layer copy is removed. Runtime after the fix: 'caws specs migrate: --from accepts v10; got \"v11\".' and 'caws events migrate: --from accepts v10; got \"v9\".'"
    recorded_at: "2026-09-17T01:41:33.455Z"
    test_nodeid: packages/caws-cli/tests/shell/help-claim-integrity.test.js
    commit_sha: "4ee762dfe8f1245b926db73d2a1f04dd9a0eee2a"