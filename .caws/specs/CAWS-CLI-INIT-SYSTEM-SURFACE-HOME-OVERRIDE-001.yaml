id: CAWS-CLI-INIT-SYSTEM-SURFACE-HOME-OVERRIDE-001
title: 'Fix systemSurfaceEnabled ignoring injectable home override (test-hermeticity gap)'
risk_tier: 3
mode: fix
lifecycle_state: active
worktree: fix-init-system-surface-home
created_at: '2026-09-08T00:35:54.547Z'
updated_at: '2026-09-08T00:43:27.247Z'
created_by_session: '96a9b85d-e209-45ad-9efb-d8522a340f79'
blast_radius:
  modules:
    - 'caws-cli/init/system-runtime'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/init/system-runtime.ts'
    - 'packages/caws-cli/tests/helpers/hook-install.js'
    - 'packages/caws-cli/tests/init/hook-install-layout.test.js'
    - 'packages/caws-cli/tests/init/init-upgrade-retrofit.test.js'
    - 'packages/caws-cli/tests/init/codex-instruction-merge.test.js'
    - packages/caws-cli/src/shell/commands/init.ts
  out: []
invariants:
  - 'systemSurfaceEnabled accepts the same injectable home override (options.home ?? machineHome()) as its sibling functions in system-runtime.ts, so tests can sandbox CAWS_HOME/home and are not affected by whether the machine running them has adopted the system runtime.'
acceptance:
  - id: A1
    given: 'a test harness that injects an isolated home directory (CAWS_HOME or an explicit home option) with no adopted system runtime surfaces'
    when: 'caws init --agent-surface <X> runs against that isolated home'
    then: 'systemSurfaceEnabled consults the injected home, not the real machine''s ~/.caws, and correctly installs a project-local hook pack'
  - id: A2
    given: 'the existing hook-install-layout.test.js, init-upgrade-retrofit.test.js, and codex-instruction-merge.test.js suites'
    when: 'run on a machine that has genuinely adopted the system runtime for claude-code/codex (e.g. via caws init adapters install)'
    then: 'all three suites still pass because their fixture setup is hermetic against real machine state'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "packages/caws-cli: npx jest tests/init/hook-install-layout.test.js tests/init/init-upgrade-retrofit.test.js tests/init/codex-instruction-merge.test.js -> 3 suites, 45 tests, all passed (previously failed on a real machine with adopted system runtime)"
    recorded_at: "2026-09-08T00:51:03.277Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "same run above executed directly on this machine which has genuinely adopted claude-code+codex system runtime (~/.caws/surfaces/{claude-code,codex}/settings.json enabled:true) -- all 3 suites pass hermetically. Full packages/caws-cli suite: npm run test:unit -> 202/202 suites, 2499/2499 tests passing"
    recorded_at: "2026-09-08T00:51:03.785Z"