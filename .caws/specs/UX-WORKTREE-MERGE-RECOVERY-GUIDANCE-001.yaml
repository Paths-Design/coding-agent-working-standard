id: UX-WORKTREE-MERGE-RECOVERY-GUIDANCE-001
title: 'Add worktree merge recovery guidance'
risk_tier: 2
mode: fix
lifecycle_state: active
worktree: worktree-merge-recovery-guidance
created_at: '2026-07-04T13:50:44.724Z'
updated_at: '2026-07-04T13:50:44.724Z'
blast_radius:
  modules:
    - packages/caws-cli/worktree-merge-shell
    - packages/caws-cli/worktree-lifecycle-writer
    - docs/cli-ux-audit
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/store/worktrees-writer.ts'
    - 'packages/caws-cli/src/shell/commands/worktree.ts'
    - 'packages/caws-cli/tests/shell/worktree-merge-recovery-guidance.test.js'
    - 'docs/api/cli.md'
    - 'docs/architecture/cli-ux-job-model-audit.md'
  out: []
invariants:
  - 'caws worktree merge --dry-run --data is read-only and emits structured readiness/recovery data for failed and successful prerequisite checks.'
  - 'Merge prerequisite refusals and git checkout/merge failures include next-command repair guidance without changing merge mutation semantics.'
  - 'Failure guidance must steer agents to CAWS inventory/cleanup plus explicit git range/status inspection instead of ad hoc hand edits.'
acceptance:
  - id: A1
    given: 'A registered worktree that is not merge-ready because its control-plane binding is incomplete.'
    when: 'An agent runs caws worktree merge <name> --dry-run --data.'
    then: 'The command exits non-zero, mutates nothing, and emits a JSON readiness payload with findings and next_commands.'
  - id: A2
    given: 'The same not-ready worktree.'
    when: 'An agent runs caws worktree merge <name> without --dry-run.'
    then: 'The refusal includes repair guidance naming dry-run --data, worktree list --data, cleanup-plan, and git status/range inspection.'
  - id: A3
    given: 'The CLI UX audit tracks merge_failed as the next item.'
    when: 'This slice is merged.'
    then: 'The audit ledger records the completed merge recovery guidance and scopes the next evidence-backed slice.'
non_functional: {}
contracts:
  - name: 'worktree-merge-recovery'
    type: behavior
    path: 'packages/caws-cli/tests/shell/worktree-merge-recovery-guidance.test.js'
