id: UX-WORKTREE-DESTROY-NOT-FOUND-HANDOFF-001
title: 'Improve worktree destroy not-found handoff'
risk_tier: 2
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge worktree-destroy-not-found-handoff at f33744731a1762607813a12b97d8dffab17f6de3'
created_at: '2026-07-04T13:45:40.118Z'
updated_at: '2026-07-04T13:48:33.352Z'
blast_radius:
  modules:
    - packages/caws-cli/worktree-lifecycle-writer
    - docs/cli-ux-audit
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/store/worktrees-writer.ts'
    - 'packages/caws-cli/tests/shell/worktree-destroy-not-found-handoff.test.js'
    - 'docs/api/cli.md'
    - 'docs/architecture/cli-ux-job-model-audit.md'
    - 'docs/command-reference.md'
  out: []
invariants:
  - 'caws worktree destroy <name> must still refuse when <name> is absent from .caws/worktrees.json; it must not infer or delete physical paths from a missing registry entry.'
  - 'The not-found refusal must name CAWS-native next commands for inventory and cleanup planning instead of leaving agents at a dead end.'
  - 'The refusal path remains non-mutating: no worktrees.json, spec YAML, events, or physical worktree changes.'
acceptance:
  - id: A1
    given: 'A CAWS repo with no registry entry for the requested worktree name.'
    when: 'An agent runs caws worktree destroy <missing-name>.'
    then: 'The command exits non-zero, reports the missing registry entry, and includes repair guidance for caws worktree list, caws worktree prune, and caws worktree cleanup-plan.'
  - id: A2
    given: 'The same repo before the failed destroy.'
    when: 'The not-found refusal is rendered.'
    then: 'The registry, specs, events, and physical worktree directories remain byte-identical.'
  - id: A3
    given: 'The CLI UX audit tracks worktree_not_found as the next slice.'
    when: 'This slice is merged.'
    then: 'The audit ledger marks the not-found handoff completed and scopes the next evidence-backed item.'
non_functional: {}
contracts:
  - name: 'worktree-destroy-not-found'
    type: behavior
    path: 'packages/caws-cli/tests/shell/worktree-destroy-not-found-handoff.test.js'
