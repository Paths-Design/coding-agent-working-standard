id: CAWS-DSH-RELEASE-121-001
title: 'Release caws-cli 12.1.0: version bump + changelog section'
risk_tier: 3
mode: chore
lifecycle_state: active
worktree: wt-release-121
created_at: '2026-08-23T21:33:23.728Z'
updated_at: '2026-08-23T21:33:47.727Z'
blast_radius:
  modules:
    - packages/caws-cli
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - packages/caws-cli/CHANGELOG.md
    - packages/caws-cli/package.json
  out: []
invariants:
  - 'Release content is deterministic: package.json version, the CHANGELOG section header, and the tag name all say 12.1.0 (CI validates tag == version == changelog section before publishing).'
  - 'The release lane touches only CHANGELOG.md and package.json; all feature/fix content landed in prior lanes (dsh surface, skip-list lockstep, suite cleanup).'
acceptance:
  - id: A1
    given: 'the release lane merged to main'
    when: 'the two files are inspected at the merge commit'
    then: 'packages/caws-cli/package.json version is 12.1.0 and packages/caws-cli/CHANGELOG.md opens with a [12.1.0] (2026-08-23) section carrying the dsh Feature entry and the fixed entries, with no [Unreleased] header remaining'
  - id: A2
    given: 'the lane range'
    when: 'git diff main..lane is inspected'
    then: 'only CHANGELOG.md and package.json are touched'
non_functional: {}
contracts: []
