id: CAWS-LEGACY-COMMAND-TEST-MIGRATION-001a
title: 'Migrate legacy command-source test authority for specs/init family'
risk_tier: 2
mode: chore
lifecycle_state: active
worktree: legacy-test-migration-specs
created_at: '2026-05-28T04:48:45.098Z'
updated_at: '2026-05-28T04:48:45.098Z'
blast_radius:
  modules:
    - packages/caws-cli/tests
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - packages/caws-cli/tests/spec-creation.test.js
    - packages/caws-cli/tests/specs-archive.test.js
    - packages/caws-cli/tests/specs-archive-collision.test.js
    - packages/caws-cli/tests/specs-close-diff.test.js
    - packages/caws-cli/tests/specs-contracts-warning.test.js
    - packages/caws-cli/tests/init.test.js
    - packages/caws-cli/tests/shell/specs-archive-edge-cases.test.js
    - packages/caws-cli/tests/shell/specs-create-guards.test.js
  out:
    - packages/caws-cli/src
    - packages/caws-kernel/src
    - packages/caws-cli/tests/agent-claim.test.js
    - packages/caws-cli/tests/agent-claim-wiring.test.js
    - packages/caws-cli/tests/enforcement/waiver-enforcement.test.js
    - packages/caws-cli/tests/waivers-prune.test.js
    - packages/caws-cli/tests/shell/specs.test.js
    - packages/caws-cli/tests/shell/specs-list-archived.test.js
    - packages/caws-cli/tests/shell/specs-prune-archive.test.js
    - packages/caws-cli/tests/shell/specs-recover-show-archived.test.js
    - packages/caws-cli/tests/shell/specs-show-worktree-cwd.test.js
    - packages/caws-cli/tests/shell/specs-migrate-command.test.js
    - packages/caws-cli/tests/shell/init-command.test.js
    - packages/caws-cli/package.json
    - packages/caws-cli/jest.config.js
    - packages/caws-cli/CHANGELOG.md
    - .github
    - .caws/policy.yaml
    - CODEOWNERS
invariants:
  - 'After this slice, zero test files under packages/caws-cli/tests/ (excluding tests already removed from main) import from packages/caws-cli/src/commands/specs.js or packages/caws-cli/src/commands/init.js or packages/caws-cli/src/utils/spec-resolver.js for the specs/init test family. (The agent-claim / waivers tests remain in scope.out; they belong to 001b.)'
  - 'This slice is test-only. No edits to packages/caws-cli/src or packages/caws-kernel/src appear in the diff. No new shell command behavior is introduced; the implementation surface is unchanged.'
  - 'Every behavior the deleted/trimmed legacy tests asserted is either (a) already covered by an existing shell test, (b) ported into a new shell test under packages/caws-cli/tests/shell/, or (c) classified as redundant / v10-removed and explicitly documented as such in the commit message. No silent coverage loss.'
  - 'No edits to packages/caws-cli/package.json, jest.config.js, CHANGELOG.md, .github/, .caws/policy.yaml, or CODEOWNERS.'
  - 'The full packages/caws-cli jest suite continues to report exactly the six pre-existing failing suites (init.test.js, agent-claim-wiring.test.js, axe/cli-accessibility.test.js, hook-packs/classify_command_calibration.test.js, integration/gates-cli.test.js, store/snapshot-vnext-facts.test.js) OR fewer. init.test.js is in scope for deletion in this slice; after deletion, the failing-suite count should drop by one (init.test.js is removed because shell/init-command.test.js covers the v11 init surface and the legacy test is currently broken by a missing scaffold/cursor-hooks dependency).'
acceptance:
  - id: A1
    given: 'tests/spec-creation.test.js (CAWS-FIRST-CONTACT-UX guards + v10 conflict-resolution describes)'
    when: 'this slice completes'
    then: 'the v10 conflict-resolution describes ("Enhanced Spec Creation with Conflict Resolution", "askConflictResolution", "specsCommand integration with conflict resolution", "CLI integration") are deleted; the worktree-association-guards and session-ownership-guards describes are either deleted (if shell/specs.test.js or other shell tests already cover them) OR ported into a new tests/shell/specs-create-guards.test.js. The legacy file no longer exists.'
  - id: A2
    given: 'tests/specs-archive.test.js (CAWSFIX-29 archive lifecycle, A1-A9 + Security + active-worktree refusal)'
    when: 'this slice completes'
    then: 'the v11-valid edge cases (byte identity, idempotent re-archive, registry shape after archive, listSpecFiles archive-dir behavior, path-traversal refusal, active-worktree refusal) are ported into tests/shell/specs-archive-edge-cases.test.js targeting runSpecsArchiveCommand. The legacy file is deleted. Existing shell coverage in shell/specs.test.js (A7-A8) and shell/specs-prune-archive.test.js is preserved unchanged.'
  - id: A3
    given: 'tests/specs-archive-collision.test.js (CAWSFIX-30 — 4 tests: A1 refuse-on-collision, A2 force-override, A3 no-registry-entry, A4 happy path)'
    when: 'this slice completes'
    then: 'A1 (refuse on collision, without --force; v11 routes through caws specs recover) and A3 (no-registry-entry legacy-tolerance case) are ported into tests/shell/specs-archive-edge-cases.test.js. A2 (force-override path) is DELETED — v11 has no --force flag; the equivalent flow is caws specs recover. A4 (happy path) is DELETED — covered by shell/specs.test.js A1. The legacy file no longer exists.'
  - id: A4
    given: 'tests/specs-close-diff.test.js (byte-preservation on close)'
    when: 'this slice completes'
    then: 'if shell/specs.test.js A5 ("preserves field order and unrelated lines byte-for-byte") covers the same assertion, the legacy file is deleted as redundant; if not, the missing byte-preservation assertion is ported into a new describe block in tests/shell/specs-archive-edge-cases.test.js and the legacy file is deleted.'
  - id: A5
    given: 'tests/specs-contracts-warning.test.js (CAWSFIX-06 — feature+empty-contracts soft warning)'
    when: 'this slice completes'
    then: 'the legacy file is DELETED as v11-architectural-shift. v11 kernel semantic rules (packages/caws-kernel/src/spec/validate-semantics.ts:27-51 — SPEC_RULES.TIER1_MISSING_CONTRACTS / TIER2_MISSING_CONTRACTS) hard-fail mode!=chore + risk_tier in {1,2} + contracts=[]; risk_tier=3 + mode=feature + contracts=[] is explicitly permitted. The legacy soft warning has no remaining governance authority. Per CAWS-SPECS-CONTRACTS-WARNING-PORT-001 closure (resolution=superseded, merge_commit=eeea87f), no replacement test is added in this slice.'
  - id: A6
    given: 'tests/init.test.js (currently failing because src/utils/finalization.js requires the missing scaffold/cursor-hooks module)'
    when: 'this slice completes'
    then: 'the legacy file is DELETED. shell/init-command.test.js covers the v11 init surface adequately. The current failure (Cannot find module ../scaffold/cursor-hooks) was a v10 residue not a v11 coverage obligation. After deletion, the post-slice jest failure count drops from 6 to 5 (init.test.js eliminated).'
  - id: A7
    given: 'the full packages/caws-cli jest suite'
    when: 'invoked from the canonical post-merge state'
    then: 'no test file imports from packages/caws-cli/src/commands/specs.js, packages/caws-cli/src/commands/init.js, or packages/caws-cli/src/utils/spec-resolver.js for the specs/init test family within this slice. (agent-claim / waiver tests are 001b scope; they may still import from src/commands/.) The failing-suite count is at most 5 (down from 6 because init.test.js is deleted), with the remaining failures being agent-claim-wiring, axe/cli-accessibility, hook-packs/classify_command_calibration, integration/gates-cli, store/snapshot-vnext-facts — all pre-existing and out of this slice''s scope.'
non_functional:
  reliability:
    - test-only migration; no production behavior changes; coverage preservation is verified by enumerating every retained behavior in the commit messages
contracts: []
