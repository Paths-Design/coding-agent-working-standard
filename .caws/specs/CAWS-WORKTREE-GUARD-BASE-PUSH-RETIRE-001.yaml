id: CAWS-WORKTREE-GUARD-BASE-PUSH-RETIRE-001
title: 'Retire worktree-guard.sh''s base-branch push block: safe under the governed merge model'
risk_tier: 2
mode: fix
lifecycle_state: draft
created_at: '2026-09-08T01:52:22.899Z'
updated_at: '2026-09-08T01:52:22.899Z'
created_by_session: 'e33f5221-a961-4418-9632-37ab8ab02449'
blast_radius:
  modules:
    - 'packages/caws-cli/templates/hook-packs/shared/worktree-guard.sh'
    - '.caws/hooks/worktree-guard.sh'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/templates/hook-packs/shared/worktree-guard.sh'
    - '.caws/hooks/worktree-guard.sh'
    - 'packages/caws-cli/tests/hooks/bats/worktree-guard-base-push.bats'
  out: []
invariants:
  - 'A plain (non-force) publish from the base branch while worktrees are active is admitted; a force-publish from the base branch remains refused unchanged'
acceptance:
  - id: A1
    given: 'worktree-guard.sh refuses an ordinary publish from the base branch whenever any worktree is active, a rule inherited unreviewed from a 2026-06-13 bulk hook migration with no documented rationale, and the guard''s own comment header calls local edits like sterling''s retirement of this exact block legitimate growth'
    when: 'this spec closes'
    then: 'the same class of retirement lands in this repo''s own template source and installed copy, the force-publish refusal remains intact, and a bats test pins both behaviors'
observability:
  - 'worktree-guard.sh stderr output when a publish is evaluated on the base branch'
rollback:
  - 'Restore the prior push-block if-statement from git history and re-decrement hook_pack_version; no state migration needed since this is a pure hook-logic change with no persisted data'
non_functional:
  security:
    - 'no security-relevant change: history-rewriting force-publish (the actual risk) remains blocked; only the ordinary publish refusal is retired'
contracts:
  - name: 'worktree-guard-base-push'
    type: behavior
