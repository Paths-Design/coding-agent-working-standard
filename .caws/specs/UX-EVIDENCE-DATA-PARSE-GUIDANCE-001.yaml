id: UX-EVIDENCE-DATA-PARSE-GUIDANCE-001
title: 'Guide evidence data JSON parse errors'
risk_tier: 2
mode: fix
lifecycle_state: active
worktree: evidence-data-parse-guidance
created_at: '2026-07-04T17:21:55.033Z'
updated_at: '2026-07-04T17:21:55.033Z'
blast_radius:
  modules:
    - 'packages/caws-cli/evidence-shell'
    - 'packages/caws-cli/commander-parser'
    - 'docs/cli-ux-audit'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/shell/register.ts'
    - 'packages/caws-cli/tests/shell/evidence-data-parse-guidance.test.js'
    - 'packages/caws-cli/tests/shell/evidence-schema.test.js'
    - 'docs/api/cli.md'
    - 'docs/architecture/cli-ux-job-model-audit.md'
  out: []
invariants:
  - 'Malformed evidence --data JSON is refused before repo/session resolution and before any event append.'
  - 'The refusal names the parser failure plus a schema command and a copy-pasteable valid --data example for the selected evidence type.'
  - 'The fix changes diagnostics only; valid evidence record behavior and kernel schema validation remain unchanged.'
acceptance:
  - id: A1
    given: 'An operator runs caws evidence record --type test with malformed --data JSON.'
    when: 'Commander parses the command.'
    then: 'The command exits 1 before mutation and prints caws evidence schema --type test plus a valid quoted --data example.'
  - id: A2
    given: 'An operator uses gate or ac evidence types with malformed --data JSON.'
    when: 'The parse fails.'
    then: 'The diagnostic points at the matching schema command and matching payload example.'
  - id: A3
    given: 'The CLI UX audit tracks parse_error as the next repair class.'
    when: 'This slice is merged.'
    then: 'The audit ledger records the completed evidence JSON parse guidance and scopes the next slice.'
non_functional: {}
contracts:
  - name: 'evidence-data-parse-guidance'
    type: behavior
    path: 'packages/caws-cli/tests/shell/evidence-data-parse-guidance.test.js'
