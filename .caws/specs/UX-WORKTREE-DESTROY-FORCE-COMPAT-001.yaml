id: UX-WORKTREE-DESTROY-FORCE-COMPAT-001
title: 'Add worktree destroy force compatibility'
risk_tier: 3
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge worktree-destroy-force-compat at f88a4f51b9c0ba6f6a8eee281b2ede931c0b10db'
created_at: '2026-07-04T19:01:28.460Z'
updated_at: '2026-07-04T19:08:09.326Z'
blast_radius:
  modules:
    - 'packages/caws-cli worktree destroy command'
    - 'CLI reference documentation'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/shell/commands/worktree.ts'
    - 'packages/caws-cli/src/shell/register.ts'
    - 'packages/caws-cli/src/shell/command-metadata.ts'
    - 'packages/caws-cli/tests/shell/worktree-destroy-force-compat.test.js'
    - 'docs/api/cli.md'
    - 'docs/command-reference.md'
    - 'docs/architecture/cli-ux-job-model-audit.md'
  out: []
invariants:
  - '`caws worktree destroy <name> --force` is parsed as a compatibility alias for --abandon-unmerged only.'
  - '`--force` never bypasses ownership, dirty checkout, missing-registry, or other guarded destroy refusals.'
  - 'Help and docs explain the narrowed force semantics so agents are handed to cleanup-plan for broader cleanup.'
acceptance:
  - id: A1
    given: 'a CAWS repository without the requested worktree in the registry'
    when: 'an operator runs caws worktree destroy missing --force'
    then: 'the command reaches the normal missing-registry handoff instead of unknown-option failure and writes no state.'
  - id: A2
    given: 'the worktree destroy command handler'
    when: 'force is supplied'
    then: 'the destroy writer receives the same abandonUnmerged intent as --abandon-unmerged.'
  - id: A3
    given: 'the built CLI command surface'
    when: 'an operator runs caws worktree destroy --help'
    then: 'help lists --force with the narrowed compatibility semantics.'
non_functional: {}
contracts:
  - name: 'worktree-destroy-force-compat'
    type: behavior
    path: 'packages/caws-cli/tests/shell/worktree-destroy-force-compat.test.js'
