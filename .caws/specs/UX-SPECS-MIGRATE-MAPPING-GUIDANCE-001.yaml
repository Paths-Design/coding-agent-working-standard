id: UX-SPECS-MIGRATE-MAPPING-GUIDANCE-001
title: 'Guide malformed specs migrate lifecycle mappings'
risk_tier: 2
mode: fix
lifecycle_state: active
worktree: specs-migrate-mapping-guidance
created_at: '2026-07-04T17:44:18.823Z'
updated_at: '2026-07-04T17:44:18.823Z'
blast_radius:
  modules:
    - 'packages/caws-cli/specs-migrate'
    - 'docs/cli-ux-audit'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/shell/commands/specs.ts'
    - 'packages/caws-cli/tests/shell/specs-migrate-mapping-guidance.test.js'
    - 'docs/architecture/cli-ux-job-model-audit.md'
    - 'docs/api/cli.md'
    - 'docs/command-reference.md'
  out: []
invariants:
  - 'Malformed lifecycle mapping files remain non-mutating composition failures.'
  - 'Parse and shape refusals include the expected JSON object schema and a copy-pasteable example.'
  - 'Diagnostics do not echo the full malformed mapping file body.'
acceptance:
  - id: A1
    given: 'A CAWS repo and an invalid JSON file passed to specs migrate --lifecycle-mapping'
    when: 'The operator runs caws specs migrate --from v10 --lifecycle-mapping <path>'
    then: 'The command exits 2, writes no migration report, names the parse failure, and prints the mapping schema plus example JSON.'
  - id: A2
    given: 'A CAWS repo and a lifecycle mapping file whose root or entries have the wrong shape'
    when: 'The operator runs caws specs migrate --from v10 --lifecycle-mapping <path>'
    then: 'The command exits 2 and prints the same schema/example guidance without mutating specs or events.'
  - id: A3
    given: 'The CLI UX audit and command docs'
    when: 'The slice lands'
    then: 'The audit marks lifecycle mapping parse guidance as implemented and the documented migrate leaf describes the mapping shape.'
non_functional: {}
contracts:
  - name: 'migrate-mapping-guidance'
    type: behavior
    path: 'packages/caws-cli/tests/shell/specs-migrate-mapping-guidance.test.js'
