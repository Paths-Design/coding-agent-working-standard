id: UX-SPECS-CLOSE-HANDOFF-001
title: 'Add specs close state handoff'
risk_tier: 2
mode: feature
lifecycle_state: active
worktree: specs-close-handoff
created_at: '2026-07-04T12:42:45.810Z'
updated_at: '2026-07-04T12:42:52.119Z'
blast_radius:
  modules:
    - packages/caws-cli/specs
    - docs/cli-ux-audit
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - packages/caws-cli/src/store/specs-writer.ts
    - packages/caws-cli/tests/shell/specs-close-handoff.test.js
    - docs/api/cli.md
    - docs/architecture/cli-ux-job-model-audit.md
  out: []
invariants:
  - 'Closing an already-closed spec remains non-mutating unless a future explicit idempotent mode is added.'
  - 'The already-closed refusal must name safe next commands for inspect, archive, and recovery.'
  - 'Active spec close behavior and spec_closed event emission remain unchanged.'
acceptance:
  - id: A1
    given: 'a spec already in lifecycle_state closed'
    when: 'an agent runs caws specs close <id>'
    then: 'the command refuses with caws specs show <id>, caws specs archive <id>, and caws specs recover <id> guidance'
  - id: A2
    given: 'a closed spec with existing resolution and closure notes'
    when: 'the close command is refused'
    then: 'the spec YAML, events log, and closure metadata are unchanged'
  - id: A3
    given: 'the CLI UX audit and specs close docs'
    when: 'the slice is complete'
    then: 'the already-closed close gap is marked implemented and docs describe the state-aware handoff'
non_functional: {}
contracts:
  - name: 'specs-close-handoff'
    type: behavior
    path: 'packages/caws-cli/tests/shell/specs-close-handoff.test.js'
