id: UX-GATES-RUN-POSITIONAL-SPEC-001
title: 'Accept positional spec id for gates run'
risk_tier: 3
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge gates-run-positional-spec at 8a4e5907675b2076936d631c3a5395d0595c8490'
created_at: '2026-07-04T19:35:42.192Z'
updated_at: '2026-07-04T19:40:05.488Z'
blast_radius:
  modules:
    - 'packages/caws-cli shell registration and metadata'
    - 'CLI UX audit and generated command reference'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/shell/command-metadata.ts'
    - 'packages/caws-cli/src/shell/register.ts'
    - 'packages/caws-cli/tests/shell/gates-run-positional-spec.test.js'
    - 'docs/command-reference.md'
    - 'docs/architecture/cli-ux-job-model-audit.md'
  out: []
invariants:
  - '`caws gates run <spec-id>` and `caws gates run --spec <spec-id>` target the same governed gate execution path.'
  - 'Supplying both positional and --spec ids is refused before policy evaluation or event writes.'
  - 'Generated command reference and audit documentation describe the positional alias without changing gate semantics.'
acceptance:
  - id: A1
    given: 'a CAWS project with an active spec and policy'
    when: 'an operator runs `caws gates run <spec-id>`'
    then: 'the command evaluates gates for that spec instead of failing with missing --spec.'
  - id: A2
    given: 'a gates run invocation that supplies both a positional spec id and --spec'
    when: 'the ids are parsed'
    then: 'the command refuses before policy evaluation or event writes with a clear ambiguity diagnostic.'
  - id: A3
    given: 'the CLI UX audit option-mismatch evidence'
    when: 'this slice lands'
    then: 'the audit records the positional gates-run alias and the remaining-evidence resample context.'
non_functional: {}
contracts:
  - name: 'gates-run-positional-spec'
    type: behavior
    path: 'packages/caws-cli/tests/shell/gates-run-positional-spec.test.js'
