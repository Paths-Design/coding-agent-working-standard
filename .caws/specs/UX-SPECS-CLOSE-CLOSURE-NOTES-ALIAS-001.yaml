id: UX-SPECS-CLOSE-CLOSURE-NOTES-ALIAS-001
title: 'Add specs close closure notes alias'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge specs-close-closure-notes at 1fc4a0f2a63d6a2a3e9163f2d1a6e724841e7388'
created_at: '2026-07-04T12:57:06.995Z'
updated_at: '2026-07-04T13:02:17.130Z'
blast_radius:
  modules:
    - packages/caws-cli/specs
    - docs/cli-ux-audit
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - packages/caws-cli/src/shell/commands/specs.ts
    - packages/caws-cli/src/shell/command-metadata.ts
    - packages/caws-cli/src/shell/register.ts
    - packages/caws-cli/tests/shell/specs-close-closure-notes-alias.test.js
    - docs/api/cli.md
    - docs/command-reference.md
    - docs/architecture/cli-ux-job-model-audit.md
  out: []
invariants:
  - '`caws specs close --closure-notes <text>` must write the same closure_notes field as --reason.'
  - 'Supplying both --reason and --closure-notes must fail before mutation rather than silently choosing one.'
  - 'Existing --reason close behavior remains unchanged.'
acceptance:
  - id: A1
    given: 'an active spec'
    when: 'an agent runs caws specs close <id> --closure-notes "..."'
    then: 'the spec closes and the YAML contains closure_notes with the supplied text'
  - id: A2
    given: 'an active spec'
    when: 'an agent supplies both --reason and --closure-notes'
    then: 'the CLI refuses with a clear conflict message and leaves spec/events unchanged'
  - id: A3
    given: 'the CLI help/docs/audit surfaces'
    when: 'the slice is complete'
    then: '--closure-notes appears in nested help and the audit marks this missing-option retry as addressed'
non_functional: {}
contracts:
  - name: 'specs-close-closure-notes'
    type: behavior
    path: 'packages/caws-cli/tests/shell/specs-close-closure-notes-alias.test.js'
