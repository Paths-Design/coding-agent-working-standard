id: CAWS-TEST-RESOLVER-ENV-INHERITANCE-001
title: 'resolver precedence test inherits the harness CLAUDE_CODE_SESSION_ID and fails for any agent-run suite'
risk_tier: 3
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge resolver-env-fix at fb76a6316453f3e1585ad626a6b03ddb6f95b7e5'
created_at: '2026-07-25T08:45:35.971Z'
updated_at: '2026-07-25T08:56:44.575Z'
blast_radius:
  modules:
    - 'caws-cli/tests/shell/session (session-resolver precedence suite)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/tests/shell/session/resolver-guard-divergence.test.js'
  out: []
invariants:
  - 'A precedence case asserts the value produced by the variables the case sets, never a variable inherited from the environment running the suite.'
  - 'The suite produces the same verdict whether or not the ambient harness exports a session id.'
acceptance:
  - id: A1
    given: 'the suite runs under a harness that exports CLAUDE_CODE_SESSION_ID (every Claude Code session)'
    when: 'the canonical-precedence case sets only CODEX_THREAD_ID and expects that value'
    then: 'the case passes, because the child shell is started with a cleared session-variable environment rather than inheriting the parent''s'
  - id: A2
    given: 'a future regression reorders the precedence chain in lib/session-id.sh'
    when: 'the suite runs'
    then: 'the case still fails, proving the isolation fix removed the env leak without making the assertion vacuous'
non_functional:
  reliability:
    - 'The suite must be environment-independent: identical verdict in CI, a bare shell, and an agent harness.'
contracts: []
