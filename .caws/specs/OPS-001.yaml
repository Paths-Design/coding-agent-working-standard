id: OPS-001
title: Allow Claude main checkout edits
risk_tier: 3
mode: chore
lifecycle_state: closed
resolution: completed
created_at: '2026-05-12T23:25:46.927Z'
updated_at: '2026-05-17T00:00:00.000Z'
blast_radius:
  modules:
    - .claude/hooks/worktree-write-guard.sh
    - .claude/hooks/session-caws-status.sh
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - .claude/hooks/
    - .caws/specs/OPS-001.yaml
  out:
    - node_modules/
    - dist/
    - build/
invariants:
  - 'Direct Write/Edit on the main checkout is permitted by the Claude PreToolUse write guard (fail-open posture preserved into v11 worktree-write-guard).'
  - 'Worktree guidance remains advisory; dangerous git operations remain guarded by block-dangerous and worktree-guard.'
acceptance:
  - id: A1
    given: 'Claude is operating from the main checkout.'
    when: 'It uses Write or Edit on a repository file.'
    then: 'The Claude PreToolUse write guard exits successfully instead of blocking for missing or active worktrees.'
  - id: A2
    given: 'Claude starts or stops a session while worktrees exist.'
    when: 'The session worktree status hooks emit guidance.'
    then: 'The guidance says worktrees are preferred and direct checkpoint edits are allowed.'
non_functional:
  reliability:
    - 'Worktree-active state is detected via dual-shape registry reads (v10 nested and v11 direct-key).'
contracts: []
