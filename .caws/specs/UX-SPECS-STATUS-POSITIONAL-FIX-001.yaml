id: UX-SPECS-STATUS-POSITIONAL-FIX-001
title: 'Fix specs status positional parsing'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-07-04T13:41:01.919Z'
updated_at: '2026-07-04T13:41:01.919Z'
blast_radius:
  modules:
    - packages/caws-cli/specs-command-parser
    - packages/caws-cli/specs-status-list-tests
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/shell/register.ts'
    - 'packages/caws-cli/tests/shell/specs-status-list.test.js'
    - 'docs/architecture/cli-ux-job-model-audit.md'
  out: []
invariants:
  - 'The specs group-level --status handoff must not consume or shadow the specs list --status leaf option.'
  - 'Spawned CLI coverage must prove both caws specs --status <state> and caws specs list --status <state> filter lifecycle state from the linked dist entrypoint.'
acceptance:
  - id: A1
    given: 'A repo with active and closed specs.'
    when: 'An agent runs caws specs list --status active from the dist CLI.'
    then: 'Only active specs are listed; closed specs are excluded.'
  - id: A2
    given: 'The same repo.'
    when: 'An agent runs caws specs --status closed from the dist CLI.'
    then: 'The group-level compatibility path still lists only closed specs.'
non_functional: {}
contracts:
  - name: 'specs-status-positional'
    type: behavior
    path: 'packages/caws-cli/tests/shell/specs-status-list.test.js'
