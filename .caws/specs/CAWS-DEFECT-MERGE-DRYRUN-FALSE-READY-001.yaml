id: CAWS-DEFECT-MERGE-DRYRUN-FALSE-READY-001
title: 'merge --dry-run reports can_proceed:true for a merge that then fails on conflict'
risk_tier: 3
mode: fix
lifecycle_state: draft
created_at: '2026-08-19T17:26:10.422Z'
updated_at: '2026-08-19T17:26:10.422Z'
blast_radius:
  modules:
    - 'caws-cli/store (worktrees-writer, mergeWorktree dry-run)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/store/worktrees-writer.ts'
    - 'packages/caws-cli/tests/store/worktree-merge-dry-run-conflict.test.js'
  out: []
invariants:
  - 'A readiness check that cannot detect the most common failure mode must say what it does NOT check, rather than reporting an unqualified ready.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'GIVEN a worktree branch that conflicts with its base WHEN caws worktree merge <name> --dry-run runs THEN it does not report an unqualified ready, and either names the conflicting paths or states explicitly that conflict detection was not performed'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'GIVEN a dry-run that reports ready WHEN the real merge runs immediately after with no intervening base movement THEN the merge succeeds'
non_functional: {}
contracts: []
