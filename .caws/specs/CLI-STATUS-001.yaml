id: CLI-STATUS-001
title: 'Slice 6b: caws status vNext read-only dashboard'
risk_tier: 3
mode: feature
lifecycle_state: closed
resolution: completed
created_at: '2026-05-14T19:37:41.593Z'
updated_at: '2026-05-17T00:00:00.000Z'
blast_radius:
  modules:
    - packages/caws-cli/src/shell/commands/status.ts
    - packages/caws-cli/src/shell/render/status.ts
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - packages/caws-cli/src/shell/commands/status.ts
    - packages/caws-cli/src/shell/render/status.ts
    - packages/caws-cli/tests/shell/status.test.js
  out:
    - .caws/policy.yaml
    - .caws/events.jsonl
invariants:
  - 'caws status is always observability; it never mutates .caws/ state.'
  - 'status reads through the v11 store snapshot and renders project, current context, claim, and doctor findings.'
acceptance:
  - id: A1
    given: 'A v11-initialized repo with at least one active spec.'
    when: 'caws status is invoked.'
    then: 'The dashboard renders sections for project, current context, claim, and doctor findings; exit code is 0.'
  - id: A2
    given: 'A repo where the store snapshot succeeds but doctor finds warnings.'
    when: 'caws status runs.'
    then: 'Findings appear in the dashboard; status exit code is not coupled to doctor exit.'
non_functional:
  reliability:
    - 'status never writes to .caws/ — observability boundary is strict.'
contracts: []
