id: WORKTREE-REPAIR-DOC-COHESION-002
title: 'Add caws worktree repair to the README + AGENTS.md current-surface command tables (sibling cohesion fix)'
risk_tier: 3
mode: doc
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge repair-doc-02 at 16fc4964aeff124bcd545dfe079c5129304db208'
created_at: '2026-06-16T00:09:46.985Z'
updated_at: '2026-06-16T00:11:20.384Z'
blast_radius:
  modules:
    - README.md
    - AGENTS.md
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - README.md
    - AGENTS.md
  out:
    - .caws/policy.yaml
    - CODEOWNERS
invariants:
  - 'README.md and AGENTS.md each carry a current-surface "Manage CAWS worktrees" command table whose worktree-subcommand enumeration MUST include repair now that the leaf shipped (PRUNE-REPAIR-WORKTREE-001). These were missed by WORKTREE-REPAIR-DOC-COHESION-001 (which covered --help + command-reference + the architecture/guide/CLAUDE doctrine); this sibling slice closes them. Identical one-line table edit in each (add repair to the create/list/bind/destroy/merge/repair-sparse/migrate-registry list).'
  - 'Scope boundary: ONLY the two authoritative present-tense surface tables. Historical/point-in-time docs that mention the pre-repair list (docs/failure-lineage.md lesson record, dated docs/reports/* transcripts) are deliberately NOT edited — they are point-in-time records and rewriting them would falsify the history they exist to preserve.'
  - 'DOC-ONLY: no runtime, registration, generated-file, or test change.'
acceptance:
  - id: A1
    given: 'The README.md and AGENTS.md worktree command tables.'
    when: 'They are read after this slice.'
    then: 'Each lists repair among the worktree subcommands; neither presents a worktree command set that omits the shipped command. No other line changes.'
  - id: A2
    given: 'The historical docs (docs/failure-lineage.md, docs/reports/*).'
    when: 'The repo is swept for stale worktree lists.'
    then: 'They are intentionally left as point-in-time records — only README + AGENTS (current-surface authority) are corrected.'
non_functional: {}
contracts: []
