id: UX-SPECS-CLOSE-NOTES-ALIAS-001
title: 'Add specs close notes alias'
risk_tier: 3
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge specs-close-notes-alias at 80d2e0722aaaf8657db36df28f30abe62b1e131c'
created_at: '2026-07-04T18:12:17.990Z'
updated_at: '2026-07-04T18:17:13.901Z'
blast_radius:
  modules:
    - packages/caws-cli/src/shell/commands
    - packages/caws-cli/src/shell/command-metadata
    - packages/caws-cli/tests/shell
    - docs/api
    - docs/architecture
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/shell/commands/specs.ts'
    - 'packages/caws-cli/src/shell/command-metadata.ts'
    - 'packages/caws-cli/tests/shell/specs-close-closure-notes-alias.test.js'
    - 'docs/api/cli.md'
    - 'docs/architecture/cli-ux-job-model-audit.md'
    - packages/caws-cli/src/shell/register.ts
    - docs/command-reference.md
  out: []
invariants:
  - 'caws specs close --notes <text> must be a compatibility alias for closure_notes, equivalent to --reason and --closure-notes.'
  - 'Supplying more than one closure-note alias must fail before spec, event, or worktree mutation.'
  - 'Nested help, generated CLI docs, and the UX audit must document the alias so agents do not rediscover it through retries.'
acceptance:
  - id: A1
    given: 'an active spec'
    when: 'an operator runs caws specs close <id> --notes <text>'
    then: 'the spec closes successfully, writes closure_notes with that text, and appends spec_closed evidence through the existing close path'
  - id: A2
    given: 'an active spec and multiple supplied note aliases among --reason, --closure-notes, and --notes'
    when: 'caws specs close is invoked'
    then: 'the command refuses before mutation and explains that only one closure-note alias may be supplied'
  - id: A3
    given: 'the CLI help/docs/audit surfaces are inspected'
    when: 'nested help and docs checks run'
    then: 'they list --notes as an alias for closure notes and the audit records this unknown-option class as fixed'
non_functional: {}
contracts:
  - name: 'specs-close-notes-alias'
    type: behavior
    path: 'packages/caws-cli/tests/shell/specs-close-closure-notes-alias.test.js'
