id: CAWS-HOOKS-GUARD-CONFIG-MACHINE-PLANE-E2E-01
title: 'Prove the machine-routed plane honors tier-2 guard config end to end'
risk_tier: 2
mode: chore
lifecycle_state: active
worktree: wt-machine-plane-e2e
created_at: '2026-09-19T01:51:52.614Z'
updated_at: '2026-09-19T01:52:03.127Z'
created_by_session: 'e625532a-0d29-4254-9cde-8e8c07b6db8d'
blast_radius:
  modules:
    - 'machine-routed hook plane: bin/caws-hook launcher + shared run-handlers guard-config load'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/tests/adapter/system-runtime.test.js'
  out: []
invariants:
  - 'The assertion must drive the REAL launcher (python3 $home/bin/caws-hook <surface> pre_tool_use --system) against an isolated CAWS_HOME, not a project-wired dispatcher — the project-wired half is already covered by tests/hooks/bats/guard-config-adopters.bats and proves nothing about caws_source_lib resolution.'
  - 'The no-config baseline must be asserted and must FAIL to admit, or the configured arm passes vacuously — the same trap that made six project-wired arms vacuous before the oracle was corrected.'
acceptance:
  - id: A1
    given: 'an isolated machine runtime installed from the current shared manifest, and a repo declaring guards.scope-guard.sh.additional_allow_prefixes'
    when: 'the identical pre_tool_use envelope is driven through bin/caws-hook with and without the document'
    then: 'the verdict differs — refused without, admitted with — proving caws_source_lib resolves guard-config.sh and its sibling parser on the machine plane'
  - id: A2
    given: 'the machine runtime install set built by runtimeFiles()'
    when: 'it is enumerated'
    then: 'it contains lib/guard-config.sh AND lib/guard-config.py, because run-handlers.sh does ''caws_source_lib guard-config.sh || return 2'' on the machine branch — a missing lib would abort the whole handler chain with exit 2 on every machine-routed tool call, not degrade'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Driven through the REAL launcher: python3 $home/bin/caws-hook codex pre_tool_use --system, against an isolated CAWS_HOME built by installMachineRuntime(). Arm 'the tier-2 document reaches a guard through bin/caws-hook, changing what it sees' is a controlled pair: identical envelope (file_path native/core.rs), identical fixture guard, the hook-policy.json document as the ONLY variable. Observed SAW:absent: before writing the document and SAW:ok:native/, after, with not.toContain('SAW:no-accessor') asserted on BOTH runs so the difference is the document and not a failure to source the lib. Run: cd packages/caws-cli && npx jest tests/adapter/system-runtime.test.js => 17/17 pass; full suite at this sha => 246/246 suites, 3285/3285 tests, exit 0. SCOPED NON-CLAIM: the arm substitutes a fixture guard for scope-guard.sh, so what is proven is the TRANSPORT and the accessor contract (launcher -> run-handlers -> shared lib -> caws_guard_prefixes), not a scope-guard ADMIT/REFUSE flip on the machine plane; the real-guard verdict flip is proven project-wired in tests/hooks/bats/guard-config-adopters.bats. The substitution was made non-trivial by sourcing the lib exactly as the three real adopters do ($SCRIPT_DIR/lib/guard-config.sh per scope-guard.sh:79, god-object-check.sh:47, loc-delta-check.sh:35) rather than via CAWS_SHARED_LIB_DIR, which is what proves runtimeFiles() reproduces the guard-beside-lib/ relative layout those adopters depend on. Non-vacuity control: arm 'a runtime MISSING the accessor is refused by name' deletes lib/guard-config.sh from the installed snapshot and observes the same envelope go from SAW:absent: to a block naming the absolute path, with no SAW: marker at all."
    recorded_at: "2026-09-19T02:08:46.321Z"
    artifact_path: packages/caws-cli/tests/adapter/system-runtime.test.js
    commit_sha: b8efc5e8