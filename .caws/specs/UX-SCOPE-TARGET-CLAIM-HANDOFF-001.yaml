id: UX-SCOPE-TARGET-CLAIM-HANDOFF-001
title: 'Clarify scope target-claim authority handoff'
risk_tier: 2
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge scope-target-claim-handoff at 6fdb5f9cb47e6f2223a190d4fe64995128f835b7'
created_at: '2026-07-04T13:58:50.723Z'
updated_at: '2026-07-04T14:03:10.918Z'
blast_radius:
  modules:
    - 'packages/caws-cli/scope-shell'
    - 'packages/caws-cli/scope-decision-renderer'
    - 'docs/cli-ux-audit'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/caws-cli/src/shell/render/decision.ts'
    - 'packages/caws-cli/src/shell/commands/scope.ts'
    - 'packages/caws-cli/tests/shell/scope-target-claim-authority-handoff.test.js'
    - 'packages/caws-cli/tests/shell/scope-show-json.test.js'
    - 'docs/api/cli.md'
    - 'docs/architecture/cli-ux-job-model-audit.md'
  out: []
invariants:
  - 'A scope decision resolved from another worktree''s scope.in claim must not report the current checkout as authoritative in JSON.'
  - 'Human, JSON, and batch plan scope outputs must hand off target-claim admits to the owning worktree context without mutating state.'
  - 'The scope kernel verdict remains unchanged: single-claimant paths can still admit, but the authority context is explicit.'
acceptance:
  - id: A1
    given: 'A path is claimed by exactly one active bound worktree while the command runs from the base checkout.'
    when: 'An agent runs caws scope show/check <path> --json.'
    then: 'The JSON reports source target_scope_in_claim, mode union, and remediation commands to inspect/list and enter the owning worktree.'
  - id: A2
    given: 'Multiple target paths are evaluated from the base checkout and at least one is admitted only through another worktree''s claim.'
    when: 'An agent runs caws scope plan --path ... --json or human output.'
    then: 'The read-only plan groups the authority-handoff commands without changing the admit/refuse counts or mutating CAWS state.'
  - id: A3
    given: 'The CLI UX audit tracks no_scope_authority as the next repair class.'
    when: 'This slice is merged.'
    then: 'The audit ledger records the completed target-claim handoff fix and scopes the next slice.'
non_functional: {}
contracts:
  - name: 'scope-target-claim-handoff'
    type: behavior
    path: 'packages/caws-cli/tests/shell/scope-target-claim-authority-handoff.test.js'
