id: WORKTREE-REPAIR-DOC-COHESION-001
title: 'Add caws worktree repair to the worktree group --help blurb + regenerate command-reference + doctrine docs (no runtime lie)'
risk_tier: 3
mode: doc
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge repair-doc-01 at 0623118563209b59f72e38969c73c5838a1f968d'
created_at: '2026-06-16T00:04:19.667Z'
updated_at: '2026-06-16T00:08:23.120Z'
blast_radius:
  modules:
    - packages/caws-cli/src/shell/command-metadata.ts
    - docs/command-reference.md
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - packages/caws-cli/src/shell/command-metadata.ts
    - docs/command-reference.md
    - docs/architecture/caws-vnext-command-surface.md
    - docs/guides/worktree-isolation.md
    - CLAUDE.md
  out:
    - .caws/policy.yaml
    - CODEOWNERS
invariants:
  - 'The worktree group --help blurb (command-metadata.ts WORKTREE_COMMAND_META.description, which drives `caws worktree --help`) enumerates its subcommands and MUST include `repair` now that the leaf shipped (PRUNE-REPAIR-WORKTREE-001). Omitting it is a runtime lie: the help text claims a subcommand set that does not match the registered leaves. The blurb is corrected to list repair alongside repair-sparse.'
  - 'docs/command-reference.md is GENERATED from COMMAND_SURFACE_METADATA via packages/caws-cli/scripts/generate-command-reference.mjs — it is regenerated (not hand-edited) so the corrected blurb AND the repair leaf body both appear. The generated file must match a fresh run of the generator (no drift between the metadata and the committed doc).'
  - 'The doctrine/prose docs that enumerate the worktree lifecycle command list (docs/architecture/caws-vnext-command-surface.md, docs/guides/worktree-isolation.md, CLAUDE.md) are updated for cohesion to include `repair` so no authoritative doc presents a command set that omits the shipped command. These are present-tense what/how/why edits — no status barnacles, no lineage prose.'
  - 'DOC-ONLY slice: no runtime behavior, no command registration, no test, no event/schema change. The repair leaf already exists and is registered; this slice only makes the descriptive surfaces (group help + generated reference + doctrine) tell the truth about it.'
acceptance:
  - id: A1
    given: 'The worktree group help, after the blurb fix.'
    when: 'caws worktree --help is run on the built binary.'
    then: 'The group description enumerates repair among the subcommands (no longer the stale create/list/bind/destroy/merge/migrate-registry/repair-sparse set that omits it), and the repair leaf is listed with its description.'
  - id: A2
    given: 'COMMAND_SURFACE_METADATA with the corrected blurb + the registered repair leaf.'
    when: 'node packages/caws-cli/scripts/generate-command-reference.mjs is run.'
    then: 'docs/command-reference.md regenerates to include the corrected worktree blurb and a `caws worktree repair` section; the committed file is byte-identical to the generator output (no manual drift).'
  - id: A3
    given: 'The doctrine docs (caws-vnext-command-surface.md, worktree-isolation.md, CLAUDE.md) that list the worktree lifecycle commands.'
    when: 'They are read after this slice.'
    then: 'Each worktree-command enumeration includes repair; none presents a list that omits the shipped command. No behavior or other surface changes.'
non_functional: {}
contracts: []
