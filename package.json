{
  "name": "caws",
  "version": "1.0.0",
  "description": "CAWS - Coding Agent Workflow System (Turborepo)",
  "website": "https://paths.design",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "// Core turbo scripts (delegated to individual packages)": "",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "lint:staged": "lint-staged",
    "format": "turbo run format",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:unit": "turbo run test",
    "validate": "turbo run validate",
    "typecheck": "turbo run typecheck || tsc --noEmit",
    "clean": "turbo run clean",

    "// Test scripts (with smart fallbacks)": "",
    "test:contract": "find packages -type d -path '*/tests/contract' | head -1 | xargs test -d && npx jest --testPathPattern=contract --passWithNoTests || echo '⚠️  Contract tests not found - run tests in packages with contract test directories'",
    "test:integration": "find packages -type d -path '*/tests/integration' | head -1 | xargs test -d && npx jest --testPathPattern=integration --passWithNoTests || echo '⚠️  Integration tests not found - run tests in packages with integration test directories'",
    "test:e2e:smoke": "find packages -type d -path '*/tests/e2e' | head -1 | xargs test -d && npx jest --testPathPattern=e2e --passWithNoTests || echo '⚠️  E2E smoke tests not found - run tests in packages with e2e test directories'",
    "test:mutation": "find packages -type d -path '*/tests/mutation' | head -1 | xargs test -d && npx jest --testPathPattern=mutation --passWithNoTests || echo '⚠️  Mutation tests not found - run tests in packages with mutation test directories'",
    "test:axe": "find packages -type d -path '*/tests/axe' | head -1 | xargs test -d && npx jest --testPathPattern=axe --passWithNoTests || echo '⚠️  Accessibility tests not found - run tests in packages with axe test directories'",
    "perf:budgets": "find packages -type d -path '*/tests' -name '*perf*' | head -1 | xargs test -d && npx jest --testPathPattern=perf --passWithNoTests || echo '⚠️  Performance budgets not found - run tests in packages with perf test directories'",

    "// CAWS CLI shortcuts (improved to actually run commands)": "",
    "cli": "node packages/caws-cli/dist/index.js --help",
    "scaffold": "node packages/caws-cli/dist/index.js scaffold",
    "init": "node packages/caws-cli/dist/index.js init",

    "// Development utilities": "",
    "hooks:install": "chmod +x .git/hooks/pre-commit .git/hooks/pre-push",
    "release": "semantic-release",

    "// CAWS-specific tools": "",
    "caws:start": "node apps/tools/caws/start.js",
    "caws:validate": "node .caws/validate.js .caws/working-spec.yaml",
    "caws:verify": "exec bash scripts/verify.sh",
    "caws:attest": "node apps/tools/caws/attest.js > .agent/attestation.json",
    "caws:prompt-lint": "node apps/tools/caws/prompt-lint.js .agent/prompts/*.md --allowlist apps/tools/caws/tools-allow.json",
    "caws:provenance": "node apps/tools/caws/provenance.js > .agent/provenance.json",
    "caws:pr-comment": "node apps/tools/caws/pr-comment.js",

    "// Code modification tools": "",
    "codemod:dry": "node docs/*/codemod/refactor.js",
    "codemod:apply": "node docs/*/codemod/refactor.js --apply"
  },
  "keywords": ["caws", "cli", "scaffolding", "agents", "workflow", "turborepo", "monorepo"],
  "author": "Paths Design <hello@paths.design>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Paths-Design/coding-agent-working-spec.git"
  },
  "packageManager": "npm@10.0.0",
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "babel-jest": "^29.0.0",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-node": "^11.1.0",
    "lint-staged": "^15.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22.14.0",
    "npm": ">=10.0.0"
  }
}
